# PILGRIM 3.0 hardening: production evidence

Carry could previously accept an old capture using its delivery time, and reset could unlock an unresolved sensor transaction. This release uses capture-time freshness and generation/request/capture ownership through settlement, with reset invalidating cached routes. A separate Astra-low worker implemented the repair, an Astra-low reviewer independently certified the critical contracts, and Terra medium handled documentation and QA evidence. The existing VO, map, world and vehicle models were preserved.

## Provenance and scope

Runtime commit `24c7e90c7794c6ab0a051fc077cf23cd90727701`; deployment `dpl_r7QhyNPNoWXv4HUFTZMoohmf3PY9`. Immutable deployment: `https://offline-event-horizon-i4i6n3qqq-sf-i455.vercel.app`; tested alias: `https://offline-vastness.vercel.app`. CI succeeded: https://github.com/Dan-Seo/offline-event-horizon/actions/runs/34424615272.

Previously verified locally: focused 24 tests, `npm test` 47/47, typecheck, build, saved eight-frame replay, and local Chrome smoke 9/9. An independent Astra-low review returned PASS. Production reports below were recorded in Chrome `152.0.7977.83` on Windows 11 / i7-12700K / RTX 3090 Ti / 31.7 GiB; they are single-machine browser observations, not device-wide performance guarantees.

An initial sandboxed WebGPU attempt was denied network access and yielded no application evidence. The same authorized suite was then run using an unrestricted browser-harness execution environment; that harness-environment change produced the successful reports below.

| Check | WebGPU (`2026-09-10T01:19:25.013Z`) | WebGL2 (`2026-09-10T01:21:56.400Z`) |
| --- | --- | --- |
| Lab checks / errors | 39/39 / 0 | 39/39 / 0 |
| Viewport, quality, DPR | 1600x1000, HIGH, 1 | 1600x1000, BATTERY, 0.85 |
| Frame interval p50 / p95 / p99 | 6.90 / 7.00 / not emitted ms | 6.90 / 7.00 / not emitted ms |
| Worker p50 / p95 | 9.10 / 15.00 ms | 8.60 / 13.90 ms |
| Calibration max / mean depth error | 0.001836 / 0.000705 (12 samples) | 0.004610 / 0.001370 (12 samples) |
| Clear tracking / ATE / RPE translation, rotation | 99.833%; 1.128; 0.153, 0.00971 rad | 99.835%; 1.620; 0.163, 0.00963 rad |
| 32-frame replay | 31/31 compared; zero translation/rotation difference | 31/31 compared; zero translation/rotation difference |

ATE is unscaled SE(3) alignment; RPE is approximately one-second relative motion. The short replay clips are near-collinear, so their ATE is unavailable; their replay RPE remains recorded in the JSON evidence.

Journey (`2026-09-10T01:26:25.056Z`): 16/16, zero errors; 1600x1000 HIGH/DPR 1, frame p50/p95/p99 6.90/7.00/7.10 ms. Quiet Carry ran 120 s, traveled 142.80 units, and held once. Keyboard W and two-thumb input immediately interrupted Carry; touch release, manual rest, planetary traversal, recovery, and bounded-state checks passed.

Ground-truth poses, renderer normals and semantic labels remain outside estimation and planning, used for evaluation, exports, QA and truth visualization. Documented actual terrain contact/recovery and a single artistic event-hold flag remain permitted physical/artistic inputs; neither truth pose nor privileged route coordinates inform pose estimation or route selection. A non-settling platform operation may pause analysis until settlement/disposal, while the ownership guard prevents stale steering or parallel capture work. The [existing model boundaries](PILGRIM.md) still apply: idealized RGB-D/IMU, local planning and mapping, no global relocalization or loop closure, and no therapeutic validation. The journey did not exercise cliff gliding; its deterministic dynamics checks are the evidence for that behavior in this pass.

Root accepted Gates D/E after all 94 production checks and personal inspection of the current water-stillness, coast, portrait, both sensor-panel, and trajectory captures. This is a rendered-evidence and automated-interaction judgment, not a human study or fresh audio test. The later docs-only commit does not change the deployed runtime.

Retained evidence: [summary](evidence/pilgrim-hardening/summary.json), [WebGPU report](evidence/pilgrim-hardening/pilgrim-lab-qa.json), [WebGL2 report](evidence/pilgrim-hardening/pilgrim-lab-qa-webgl.json), [journey report](evidence/pilgrim-hardening/pilgrim-journey.json), [WebGPU replay](evidence/pilgrim-hardening/lab-sequence-replay.json), [WebGL2 replay](evidence/pilgrim-hardening/lab-sequence-webgl-replay.json), [compressed WebGPU clip](evidence/pilgrim-hardening/clear-32-frames.tar.gz), [WebGPU lab](evidence/pilgrim-hardening/lab-webgpu.jpg), [WebGL2 lab](evidence/pilgrim-hardening/lab-webgl.jpg), [healing stillness](evidence/pilgrim-hardening/healing-stillness.jpg), and [healing portrait](evidence/pilgrim-hardening/healing-portrait.jpg).

Replay the retained current production capture with `npm run replay:rgbd -- docs/evidence/pilgrim-hardening/clear-32-frames.tar.gz`. Compression round-trip matched the original TAR SHA-256 `fc16dfae918260c4e6f8db96383358677dccd1748d5b233acf5a832cd6da3018`.

Horn's absolute-orientation solve now stops its Jacobi sweeps at a tolerance relative to the matrix's own Frobenius norm rather than a fixed absolute one, which ends the sweeps at a slightly different point. Replaying `tests/fixtures/clear-eight-frames.tar.gz` against the current tree reports a maximum translation difference of about 1.3e-10 instead of exactly 0, and its drift fraction moves in the eleventh decimal, from 0.06660124275924947 to 0.06660124274743297. The reports recorded above, including the zero replay differences, were produced before that change and ran through the same solver, so they are no longer bit-exact against the current tree; the 32-frame clips were not re-run in this pass. Every gate, tracking figure and accepted verdict above is unaffected — the shift is nine orders below the tightest gate.

CI browser smoke (2026-09-11): Playwright's headless shell exposes no WebGPU at all, and the bundled Chromium falls back to WebGL2 on a GPU-less runner (4/9 in that experiment). New headless Chromium launched with `--enable-unsafe-webgpu --enable-unsafe-swiftshader` does expose a SwiftShader WebGPU adapter, but headless Chromium on Linux loses that device immediately ("A valid external Instance reference no longer exists", the same in eight flag combinations and in Chrome stable), while the same launch headed under `xvfb-run` boots the world on WebGPU with no errors; so the `webgpu-smoke` CI job runs `qa:pilgrim` headed under xvfb with `QA_SOFTWARE_GPU=1` and `QA_HEADED=1`. Locally on that adapter the smoke detects backend WebGPU with no shader or runtime errors; boarding, the drag override and the shared planner pass, while the five wall-clock checks (W+D acceleration, rest settling, live Carry frames, lab captures, tracking after 3.5 s of W) cannot hold because one RGB-D readback takes 2–7 s on SwiftShader against a 1/30 s simulation step. Those five are recorded in the report with `skipped: "software GPU"` and do not gate CI; the real-time behaviour stays verified by the installed-Chrome suites in this document.

## September 22: Carry clarity and independent Fable review

Baseline `9e02ad6`; first implementation `a75f9f5`. These results are distinct from the September 10 reports above. The baseline passed 89 tests, build, 16 journey checks and 41 lab checks in installed Chrome 153.0.8010.53. The first implementation passed 93 tests, lint, build, 17 journey checks and 41 lab checks. Its 120-second quiet Carry traveled 142.49 units, scheduled one scenic rest and entered no failed-search hold in that interval. Subsequent coastal traversal, manual handover, touch release and recovery passed. A single unit corresponds to one meter in the metric sensor contract.

The actual first-candidate 32-frame turning recording replays all 31 comparable relative transforms: maximum translation difference 3.55e-15, rotation difference zero. Against separately recorded truth it has 31 tracked / 32 total, zero LOST, distance 13.2013 m, unscaled SE(3) ATE 0.09709 m, aligned orientation RMS 0.38116 rad, one-second RPE translation 0.15710 m / rotation 0.009461 rad, 23 pairs (interval tolerance 0.26 s), endpoint drift 0.27585 m. The opposite-turn holdout also passes all 41 lab checks and replays 31 pairs; ATE 0.10495 m, aligned orientation RMS 0.44767 rad, RPE translation 0.19107 m / rotation 0.010131 rad, zero LOST. Both are 32-frame, 192x128 RGB-D recordings with no scale fit. The holdout used `QA_TURN=left npm run qa:lab` (PowerShell: `$env:QA_TURN='left'; npm run qa:lab`). Neither algorithm nor threshold was tuned after seeing it.

Replay agreement is reproducibility; separately recorded truth comparisons are accuracy evidence. These short curved paths pass the existing conditioning test but their orientation alignment remains weak; they do not establish global localization accuracy. The initial STARTING sample is counted in the denominator and is not called LOST. The accuracy gates remain lost fraction <=0.1, RPE translation <=0.5 m, endpoint drift / distance <=0.2. Stationary/near-collinear ATE remains null. The unchanged tests independently recover known metric transforms, reject outliers, enforce units, and handle missing tracking and restarted segments. The new actual-worker replay test produces identical VO, routes and director commands with truth absent or corrupted, including deliberately corrupted labels and normals.

Fable B reviewed `a75f9f5` and the actual before/after images and returned REVISE. Both findings reproduced in failing tests: a beauty event overwrote a blocked hold with `watching` until the old cooldown expired; a stale render interval erased confirmation between two individually fresh safe captures. Astra separated the event-watch timer from the failed-search timer and removed only that frame-driven reset; stale or unsafe incoming observations and lifecycle resets still clear confirmation. The new tests also check that a new ride cannot inherit an old watch. All 95 tests and lint pass after repair. Fable requested that browser QA read the actual rest whisper rather than restating the getter's definition. The journey check now samples the DOM, allowing for React's periodic snapshot update. Fable judged the quieter material legible in the actual starting-sea screenshots; this was screenshot review, not Fable browser interaction or a human comfort study.

### Final candidate `64306e9`: review, lifecycle and performance

The repaired runtime is `64306e97bbe632a7e06006842e1f49b8d53b7520`. All 95 tests, lint, production build, three Python reconstruction checks and [GitHub CI](https://github.com/Dan-Seo/offline-event-horizon/actions/runs/35707896308) passed. Existing known-motion tests recover independent metric transforms; the eight-frame fixture remains short-clip determinism evidence. The runtime payload-exclusion test and actual-worker truth-perturbation test establish different parts of the boundary: privileged fields are removed before dispatch, and deliberately added truth/labels/normals cannot change worker estimates, routes or director commands. Neither test proves realistic sensor noise or global SLAM.

[Fable B2/C](evidence/fable-pass/fable-C.md) accepted this exact revision, with disclosed limits. After an interactive PreToolUse hook stalled, Astra cancelled that request without claiming a verdict. A bounded one-turn CLI request resumed the same explicit Claude session using existing OAuth, no built-in tools and an empty MCP list. Hooks/settings were not disabled. Tool metadata returned canonical model `claude-fable-5-1`, provider `firstParty`; this is execution metadata, not server attestation. Fable reviewed the supplied diff/results, not the browser. Its suggestion that short-path alignment explains the high aligned-orientation RMS remains an inference; its proposed doubled-path experiment was not performed. The English DOM-whisper check explicitly fixes `en-US`.

Installed Chrome `153.0.8010.53` reported an NVIDIA Ampere WebGPU adapter on this Windows workstation (known RTX 3090 Ti). No SwiftShader result is presented as hardware performance. A portrait 4 Hz lifecycle run passed 24 checks: manual override, capture suspension during ordinary manual use, five Carry/rest cycles, one reused worker, worker cleanup after leaving the lab, LOW_LIGHT tracking loss followed by CLEAR recovery, and recovery after reset. Manual cancellation was observed within 30 ms in that run. Unit tests cover stale generations, pending capture ownership, late results, worker failures and disposal. A forced physical GPU device loss was not tested.

| Same-condition measurement | Baseline `9e02ad6` | Candidate | Interpretation |
| --- | --- | --- | --- |
| Manual-rest frame interval p50 / p95 / p99 | 6.9 / 7.0 / 7.1 ms | 6.9 / 7.0 / 7.1 ms | Chrome, WebGPU, 1600x1000, HIGH, DPR 1, serial runs. |
| Manual-rest GPU render-pass p50 / p95 / p99 | 0.458752 / 0.524288 / 0.589824 ms | 0.458752 / 0.589824 / 0.589824 ms | Actual GPU timestamp samples, quantized; not whole-frame GPU time. No speedup claim. |
| Lab CPU worker p50 / p95 | 10.6 / 15.5 ms | 10.7 / 18.6 ms | One run each; estimator unchanged, no causal performance claim. |
| 120 s quiet Carry distance | 142.983 m | 142.487 m (first), 141.250 m (final preview) | Final preview scheduled one scenic rest, no failed-search holds. Does not establish general autonomy success. |
| Quiet Carry frame p50 / p95 / p99 | 6.9 / 7.0 / 7.1 ms | 6.9 / 7.0 / 7.1 ms | Final preview max 14 ms; no interval over 50 ms. |

Candidate lab readback p50/p95/p99 was 3.3/5.7/8.7 ms. The separate portrait 4 Hz run measured 2.8/20.6/31.2 ms (maximum 33.8 ms). These are samples of the latest readback result, not every capture or end-to-end capture-to-control latency. CPU worker and GPU render-pass measurements are separate. Single-run differences do not establish an optimization.

Twenty stationary Carry toggles retained one worker, 130 DOM nodes, one document and a contact cache of 527. After GC, main-thread JS heap grew from 19,794,528 to 28,573,792 bytes (+8,779,264). A matched sensor-free manual control retained zero workers, 128 DOM nodes, one document and the same contact-cache size; heap grew from 18,964,328 to 27,853,376 bytes (+8,889,048). This comparison did not detect additional Carry-specific growth. Shared growth remains unexplained; VRAM, leak freedom and multi-hour stability are not certified.

The protected immutable preview is `https://offline-event-horizon-gefhrta9q-sf-i455.vercel.app`, deployment `dpl_5NZXNY9uzmumMFvN8jkRmCVor9Rr`, built from runtime `64306e9`. Preview journey passed 16/17 and lab 40/41: every application assertion passed, but Vercel's injected `feedback.js` violated the unchanged self-only script CSP. The console gate was retained and the policy was not weakened. Public production must be checked separately; preview is not reported as an all-pass run.

A separate, user-requested visual reviewer personally operated local `64306e9` at 1600x1000 / WebGPU / HIGH / DPR 1, including Go, Rest, and normal UI travel to Breathing Forest. Sampled views had no warning/error messages or confirmed material regression. The candidate craft remained legible with a cooler, quieter material; transient bottom-edge framing during boarding ended once settled. Flat-looking forest canopy and sparse foreground detail were recorded as later candidates, outside this release. Sparse images cannot certify temporal shimmer or hitch absence. This is visual review, not a human comfort study, physical mobile test or audio evaluation.

Retained turning clips: [right](evidence/fable-pass/right-32-frames.tar.gz), [opposite-turn holdout](evidence/fable-pass/left-32-frames.tar.gz), [capture identities](evidence/fable-pass/captures.json), [right replay](evidence/fable-pass/candidate-replay.json), [left replay](evidence/fable-pass/holdout-replay.json). Run `npm run replay:rgbd -- docs/evidence/fable-pass/right-32-frames.tar.gz` (or the left file). Both gzip round-trips match the tested TAR hashes. [Summary](evidence/fable-pass/summary.json), [baseline material](evidence/fable-pass/baseline-manual.jpg), [candidate material](evidence/fable-pass/candidate-manual.jpg), [Fable A2](evidence/fable-pass/fable-A2.md), [Fable B](evidence/fable-pass/fable-B.md), and [Jev triage](evidence/fable-pass/jev-A.json) distinguish model opinions from test evidence. Sensitive raw provider/authentication logs are excluded.

[Fable D](evidence/fable-pass/fable-D.md) accepted the exact candidate on September 22, explicitly requiring public-production checks before release acceptance. It also required the existing fallback limitation to remain explicit: while VO is LOST, stale internal pose cannot establish a held hull; bounded current-depth movement (at most 4 m/s) relies on observed near-field rejection and physical contact/recovery. This is limited local fallback, not reliable localization or guaranteed progress. A stationary no-toggle memory control suggested by D was not run; the matched Carry/manual comparison above is the available evidence.

### Public-production acceptance

Production `dpl_DsqUqYYtJekVNyey8ZSvneZm9FDH` is READY at [the public alias](https://offline-vastness.vercel.app) and [immutable URL](https://offline-event-horizon-hulpbadfy-sf-i455.vercel.app). Vercel rebuilt the reviewed source during promotion; authenticated metadata confirms source/git commit `64306e97bbe632a7e06006842e1f49b8d53b7520` and original preview deployment. Six of eight entry-script hashes match, while Next/React bootstrap and Turbopack loader differ. [Artifact identity](evidence/fable-pass/production-artifact.json) retains all hashes and the provenance limitation: the upload-time dirty-path inventory was not captured atomically. A subsequent clean-tracked-tree snapshot had only the survey document and evidence directory untracked. It is not proof of a complete historical upload inventory. Framework-chunk semantic equivalence is not claimed.

| Public suite, September 22 UTC | Result | Actual configuration / observation |
| --- | --- | --- |
| Journey, 09:41:45 | 17/17, zero errors | WebGPU HIGH/DPR 1, 1600x1000; 120 s, 141.656 m, one intentional rest, zero failed-search holds; DOM whisper and immediate keyboard/touch handover pass. |
| Research Lens, 09:43:54 | 41/41, zero errors | WebGPU HIGH/DPR 1; synchronized capture, bounded recording, export, estimation, fault/recovery and viewport controls pass. |
| Research Lens fallback, 09:46:09 | 41/41, zero errors | WebGL2 BATTERY/DPR 0.85; LOW_LIGHT LOST to CLEAR TRACKING and all remaining assertions pass. |

All 99 public checks passed. The preview-only Vercel feedback-script CSP failure did not recur on the public alias; CSP and console assertions remain unchanged. Root inspected actual public [water](evidence/fable-pass/production-water-stillness.jpg), [coast](evidence/fable-pass/production-aurora-coast.jpg), [portrait](evidence/fable-pass/production-portrait.jpg), [WebGPU lab](evidence/fable-pass/production-lab.jpg) and WebGL2 lab captures produced through ordinary controls.

Public Journey frame interval p50/p95/p99 is 6.9/7.0/7.1 ms (17,488 samples, maximum 13.9 ms, none over 50 ms). Public WebGPU lab render-pass GPU p50/p95/p99 is 0.524288/0.720896/0.720896 ms; CPU worker is 9.0/14.7/20.5 ms, sampled readback 2.6/3.5/5.3 ms. WebGL2 has no GPU timestamp samples, and its different quality/DPR makes it unsuitable as an equal-settings performance comparison.

The full public WebGPU clear path contains 383 samples, 382 tracked, zero LOST, 182.938 m distance, unscaled SE(3) ATE 1.44597 m, RPE translation 0.199831 m / rotation 0.011570 rad over 369 one-second pairs, endpoint drift fraction 0.027983. This longer-path accumulated error is larger than the short-clip ATE; ATE is not an acceptance gate or a precision guarantee. Public 32-frame WebGPU/WebGL2 exports each replay 31 relative transforms, with maximum translation differences 0 / 1.7763568394002505e-15 and rotation differences 0 / 0. Both existing accuracy gates pass; each has 31 TRACKING, one STARTING and zero LOST. These additional raw production captures remain in ignored local artifacts; the two retained turning clips above are the repository's reproducible samples.

[Fable D2](evidence/fable-pass/fable-D2.md) accepted the source plus independently tested serving artifact, correcting the earlier byte-identical-promotion assumption. The packet's rounded-zero WebGL2 replay claim is corrected explicitly in that record; the original numeric reports are authoritative. Fable evaluated supplied evidence and metadata, not these new public images or direct browser interaction. Astra accepts this bounded release with the preceding hardware, memory, alignment and fallback limitations. Later reporting-only changes do not change the deployed runtime.
